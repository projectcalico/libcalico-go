
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  creationTimestamp: null
  name: nodebgpstatuses.crd.projectcalico.org
spec:
  group: crd.projectcalico.org
  names:
    kind: NodeBGPStatus
    listKind: NodeBGPStatusList
    plural: nodebgpstatuses
    singular: nodebgpstatus
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          status:
            description: NodeBGPStatusStatus defines the observed state of NodeBGPStatus
            properties:
              conditions:
                description: Conditions represents the latest observed set of conditions
                  for this component.
                items:
                  description: NodeBGPStatusCondition contains the status for a NodeBGPStatus
                    resource.
                  properties:
                    info:
                      description: The reason it's in the current state.
                      type: string
                    peerIP:
                      description: IP address of the peer whose condition we are reporting.
                        If port number is given, format should be `[<IPv6>]:port`
                        or `<IPv4>:<port>` for IPv4. If optional port number is not
                        set, and this peer IP and ASNumber belongs to a calico/node
                        with ListenPort set in BGPConfiguration, then we use that
                        port to peer.
                      type: string
                    since:
                      description: Since is the time since the condition last changed.
                      type: string
                    state:
                      description: The state is the bgp session state.
                      type: string
                    type:
                      description: The type is type of bgp session state.
                      type: string
                  type: object
                type: array
              numEstablished:
                description: The total number of established bgp sessions.
                type: integer
              numNotEstablished:
                description: The total number of non-established bgp sessions.
                type: integer
            required:
            - conditions
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
