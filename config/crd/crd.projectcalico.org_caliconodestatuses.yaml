
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  creationTimestamp: null
  name: caliconodestatuses.crd.projectcalico.org
spec:
  group: crd.projectcalico.org
  names:
    kind: CalicoNodeStatus
    listKind: CalicoNodeStatusList
    plural: caliconodestatuses
    singular: caliconodestatus
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CalicoNodeStatusSpec contains the specification for a CalicoNodeStatus
              resource.
            properties:
              classes:
                description: Classes specify type of information CalicoNodeStatus
                  should contain.
                items:
                  type: string
                type: array
              nodeName:
                description: The node name identifies the Calico node instance for
                  node status.
                type: string
              updateIntervalInSeconds:
                description: 'UpdateIntervalInSeconds is the period at which CalicoNodeStatus
                  should be updated. Set to 0 to disable CalicoNodeStatus refresh.
                  [Default: 10]'
                type: integer
            required:
            - nodeName
            type: object
          status:
            description: CalicoNodeStatusStatus defines the observed state of CalicoNodeStatus.
              No validation needed for status since it is updated by Calico.
            properties:
              additionalInfo:
                description: AdditionalInfo is a a human-readable description of the
                  status of last update.
                type: string
              bgpStatus:
                description: BGPStatus holds node BGP status.
                properties:
                  birdStatus:
                    description: BirdStatus represents the latest observed status
                      of bird.
                    properties:
                      lastBootTime:
                        description: LastBootTime holds the value of lastBootTime
                          from birdctl output.
                        type: string
                      lastReconfigTime:
                        description: LastReconfigTime holds the value of lastReconfigTime
                          from birdctl output.
                        type: string
                      ready:
                        description: Ready indicates if bird status is ready.
                        type: boolean
                      routeID:
                        description: Route ID used by bird.
                        type: string
                      serverTime:
                        description: ServerTime holds the value of serverTime from
                          birdctl output.
                        type: string
                      version:
                        description: Bird version.
                        type: string
                    type: object
                  numEstablished:
                    description: The total number of established bgp sessions.
                    type: integer
                  numNotEstablished:
                    description: The total number of non-established bgp sessions.
                    type: integer
                  peers:
                    description: Peers represents BGP peers status on the node.
                    items:
                      description: CalicoNodePeers contains the status of BGP peers
                        on the node.
                      properties:
                        info:
                          description: The reason it's in the current state.
                          type: string
                        peerIP:
                          description: IP address of the peer whose condition we are
                            reporting. If port number is given, format should be `[<IPv6>]:port`
                            or `<IPv4>:<port>` for IPv4. If optional port number is
                            not set, and this peer IP and ASNumber belongs to a calico/node
                            with ListenPort set in BGPConfiguration, then we use that
                            port to peer.
                          type: string
                        since:
                          description: Since is the time since the condition last
                            changed.
                          type: string
                        state:
                          description: The state is the bgp session state.
                          type: string
                        type:
                          description: The type is type of bgp session state.
                          type: string
                      type: object
                    type: array
                  routes:
                    description: Routes represents routes on the node.
                    items:
                      description: CalicoNodePeers contains the status of BGP routes
                        on the node.
                      properties:
                        destination:
                          description: Destination of the route.
                          type: string
                        gateway:
                          description: Gateway for the destination.
                          type: string
                        installedBy:
                          description: InstalledBy indicates who installed this route.
                            If it is populated by a BGP peer, this is the name of
                            the BGPPeer object. If it is populated by node mesh, this
                            is the name of the node.
                          type: string
                        interface:
                          description: Interface for the destination
                          type: string
                      type: object
                    type: array
                type: object
              updateTimestamp:
                description: UpdateTimestamp is a timestamp representing the server
                  time when CalicoNodeStatus object last updated. It is represented
                  in RFC3339 form and is in UTC.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
